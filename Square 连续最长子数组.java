/*
1. 给一个数组，找出一个长度最长的子数组，里面的值是连续的。 
Follow up: 连续的值可以跳着的，不一定index也是连续，返回这种子序列的最大长度。 

*/


public List<Integer> maxContinList (int[] nums) {
  //two pointer start end, to get the result
}


follow up:

int upper = -1
int lower = -1;
maxLen = Math.max(maxLen, upper - lower);
